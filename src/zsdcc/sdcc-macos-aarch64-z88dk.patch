Index: src/SDCCutil.c
===================================================================
--- src/SDCCutil.c	(revision 15248)
+++ src/SDCCutil.c	(working copy)
@@ -745,12 +745,15 @@
 #elif defined(__BORLANDC__)
   return "BORLANDC";
 #elif defined(__APPLE__)
 #if defined(__i386__)
-  return "Mac OS X i386";
+  return "macOS i386";
 #elif defined(__x86_64__)
-  return "Mac OS X x86_64";
+  return "macOS x86_64";
+#elif defined(__aarch64__) || defined(__arm64__)
+  return "macOS aarch64";
 #else
-  return "Mac OS X ppc";
+  return "macOS";
 #endif
 #elif defined(__linux__)
   return "Linux";
